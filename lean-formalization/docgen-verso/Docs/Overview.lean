/-
Agent Manifesto - Overview section
-/

import VersoManual

open Verso.Genre Manual

#doc (Manual) "Axiom Overview" =>

The base theory T0 consists of axioms T1 through T8. These are
"undeniable, technology-independent facts" that form the foundation
which does not shrink during revision loops.

# Axiom Categories

*Environment-derived (T1--T3, T7):*
Physical constraints of hardware and computational resources.
Sessions are bounded, context is finite, resources are limited.

*Natural-science-derived (T4--T5):*
Non-determinism inherent in generation processes (T4) and the
fundamental control-theory principle that improvement requires
feedback (T5).

*Contract-derived (T6, T8):*
Authority structures agreed upon with humans. Humans hold final
decision authority over resources (T6), and tasks have precision
levels (T8).

# Boundary Conditions

Six boundary conditions L1--L6 constrain the system:

- *L1* (Ethics/Safety): Fixed boundary, never violated
- *L2* (Scope): Task scope limitations
- *L3* (Resources): Resource constraints
- *L4* (Knowledge): Knowledge boundary awareness
- *L5* (Time): Temporal constraints
- *L6* (Quality): Output quality requirements
